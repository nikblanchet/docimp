/**
 * Valid config with jsdocStyle configuration.
 */
module.exports = {
  jsdocStyle: {
    preferredTags: { return: 'returns', arg: 'param' },
    requireDescriptions: true,
    requireExamples: 'public',
    enforceTypes: true,
  },
};
