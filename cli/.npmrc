# eslint-plugin-promise@7.2.1 declares peer dep eslint: "^7||^8||^9" but works fine with ESLint 10.
# Remove this once eslint-plugin-promise releases a version that formally supports ESLint 10.
legacy-peer-deps=true
